# Cat-Fact
A simple .NET app generating facts about cats.  

## Features
- gets one fact from endpoint: https://catfact.ninja/fact  
- writes (appends) to text file: facts.txt by default  
- creates a file if it does not exist

## Usage
Use following command in app directory: dotnet run
